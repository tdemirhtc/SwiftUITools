//
//  AnimationType1.swift
//  SUICustomToolbar
//
//  Created by Hatice Taşdemir on 8.11.2024.
//

import SwiftUI

/* struct AnimationType1: View {
    @State private var scale = 1.0
    @State private var big = false
    
    var body: some View {
        VStack {
            Text("Hello, world!")
            //yazının büyüyüp küçülmesi için scaleEffecte scale değişkeni vermek yeterli
                .scaleEffect(scale)
            //tıklandığında ne olacak:
            onTapGesture {
                //yavaş yavaş küçülecek anim ile
                withAnimation(.easeInOut(duration: 1))
                //büyükse küçülsün küçük ise büyüsün için state değişken yapar,
                if big {
                    scale = 1.0
                    
                }else{
                    scale = 4.0
                }
               //eğer her seferinde true ise false, false ise true yapacak ,
                big.toggle()
            }
            
        }
        .padding()
    }
}


#Preview {
    AnimationType1()
}*/
